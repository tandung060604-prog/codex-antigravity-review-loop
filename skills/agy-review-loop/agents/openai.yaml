interface:
  display_name: "Antigravity Review Loop"
  short_description: "Run bounded, structured Antigravity review loops"
  default_prompt: "Use $agy-review-loop to classify this task, delegate bounded structured rounds to Antigravity CLI, verify the actual diff and checks, and escalate only when evidence requires it."

policy:
  allow_implicit_invocation: false
