# Backend bug-fix example

```text
$agy-review-loop

Fix the expired-session bug in the current repository. Reproduce it first, trace the shared root cause, add the smallest regression test, and run the relevant lint, type-check, and test commands. Preserve unrelated changes. Do not commit, push, deploy, or add a new production dependency.
```
