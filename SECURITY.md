# Security policy

Do not open a public issue for credentials, tokens, private repository contents, or an exploitable vulnerability. Use a private GitHub Security Advisory for the published repository or contact the maintainer privately through the account that owns the repository.

The skill is designed to avoid credential and permission bypasses. Report any prompt or workflow that causes it to expose secrets, bypass a user limit, or perform an unrequested external action.
